/**
 * XR Stage 5 — UI Subsystem Public API
 *
 * Import from here to access the full UI layer.
 * All exports are safe to use in CLI, TUI, and daemon contexts.
 */

// Theme tokens
export * from "./theme.ts";

// Layout primitives
export * from "./layout.ts";

// Spinner / progress
export * from "./spinner.ts";
