# XR Phase 4 — Step 2: Gap Analysis

Constitution articles in force: **Art. IX** (authority≠intelligence; isolation follows risk; policy≠confinement; claims need evidence), **Art. XIV** (untrusted code isolated by risk tier; installation≠trust), **Art. XXII** (signed reproducible releases), **Art. XXI** (locality), **Art. IV** (fail closed; no `any`/empty-catch on boundaries).

| # | Gap (from Audit) | Task | Risk tier | Enforcement point | Proving test (effect-asserting) |
|---|---|---|---|---|---|
| 1 | G1 — canonical loop lacks `runIsolated`; shell falls back to host authority; TrustService opt-in | T1/T3 | tier2 | Envelope → loop toolCtx; `AgentService` wires `Tokens.Trust` unconditionally | `test/trust/canonical-shell-isolation.test.ts`: run the real agent loop with a shell call; assert the command executed inside a namespace sandbox (placement recorded) and that with the sandbox backend removed + hardened mode, the shell call is **blocked** (no host-authority run) |
| 2 | G2 — no escalate-only lattice | T1 | tier2 (policy) | `lattice.ts` merged in `TrustService.evaluate` and `decidePlacement` | `test/trust/lattice.test.ts`: capability requests tier0 then tier2 → effective tier2 persists (escalate-only); a request to downgrade after tier2 is refused; merge picks the more restrictive of (classified, requested, lattice) |
| 3 | G3 — MCP `XR_MCP_ALLOW_UNISOLATED` fail-open; unisolated HTTP; weak default-deny | T3 | tier2 | `mcp/client.ts` + `McpManager` | `test/trust/mcp-isolation.test.ts` (extend): hardened mode → unisolated spawn refused even with env var; `test/mcp/default-deny.test.ts`: server with no explicit grant exposes no tools |
| 4 | G4 — no centralized egress | T4 | tier1/tier2 | `security/egress-proxy.ts` wired into `web.ts` + plugin net capability + `TrustService` grant net checks | `test/security/egress-proxy.test.ts`: local HTTP server on 127.0.0.1 blocked (private range), `http://169.254.169.254/` blocked, `[::1]` blocked, redirect to 127.0.0.1 blocked after revalidation, byte cap enforced, DNS-resolve-then-check rejects private results (injectable resolver) |
| 5 | G5 — process-global env secrets; raw secrets into plugin workers | T4 | tier1 | CredentialBroker mediation; `worker-loader` sends names only; daemon/`hydrateSecrets` brokered; `util/process.ts` children get explicit env on trust paths | `test/trust/credentials.test.ts` (extend): broker refs never serialize raw values; worker bootstrap payload contains no raw secret; child processes of sandboxed runs have no secret env vars; logs redacted |
| 6 | G6 — dashboard auth/CSP/CSRF/rate/route caps/innerHTML | T5 | tier1 | `daemon/server.ts`, `routes/router.ts`, `dashboard/client-script.ts` | `test/daemon/hardening.test.ts`: CSP header has no `unsafe-inline` and a valid script hash/nonce; `?token=` no longer accepted after bootstrap (cookie session); Origin-mismatched mutating POST rejected; >N req/s → 429; body size cap; no `innerHTML` in the served script bundle |
| 7 | G7 — no supply chain | T6 | tier1 (release) | `.github/workflows/release.yml` + `supply-chain.yml` + `scripts/sbom.ts` | `test/supply-chain/sbom.test.ts`: generated CycloneDX parses, lists locked deps, has hashes; `scripts/verify-release.ts` verifies a locally signed blob (test key) and fails on tamper; CI job definitions assert scans gate the release |
| 8 | G8 — `node:vm` as boundary wording | T8 | truth | docs + code headers | `test/security/nodevm-wording.test.ts`: grep across `src/` and `docs/security` fails if `node:vm`/`VM sandbox` is described as a security boundary/isolation in the security sense |
| 9 | G9 — no guarantee matrix / pentest register / docs | T2/T7/docs | truth | `scripts/guarantee-matrix.ts` probing live backends | `test/trust/guarantee-matrix.test.ts`: matrix JSON reflects live `EnvironmentManager.capabilities()`; `xr trust matrix` emits it; no unsupported claim (matrix flags `enforcedNetwork:false` etc.) |

**Non-goals (documented, not implemented — Phase 4 forbids net-new features):** gVisor/Firecracker/Kata as *working* backends on this host (detection hooks + fail-closed + documented gap only; wiring real microVM orchestration is Phase 5+); Seatbelt on macOS (detection stub + documented gap; no macOS runner available to validate); Windows container/VM backend (documented gap); enterprise identity (Phase 10); SOC2/ISO certification (Phase 10, auditor-required).

**Deferred-by-design:** placement enforcement is additive behind the existing trust gate (no schema change, no data migration — Phase 4 MIGRATION_REQUIREMENTS §17 satisfied: brokering is compat-preserving via `getSecret*` retained; MCP default-deny gets an explicit-allow path via declared/granted permissions).
