---
id: git_commit_message
version: 1
source: preloaded
tools: [shell]
verifier: user_approved
signed: false
---
# Git Commit Message
1. `shell` run `git diff --staged` (approval-gated, read-only diff).
2. Write a conventional-commit message (type(scope): summary + body).
