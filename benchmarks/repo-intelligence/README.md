# Repo Intelligence benchmarks

Runner: `bun run scripts/repo-intelligence-bench.ts`

Output: `benchmarks/repo-intelligence/latest.json`

Measures initial index, incremental index (unchanged tree), query p50/p95,
and repo-map generation p50/p95 plus approximate token count.
